npm i -g @nestjs/cli

yarn start
yarn start:dev


// это уже есть в packeges.json
yarn add typeorm --save
yarn add reflect-metadata --save
yarn add @types/node --save-dev
yarn add pg --save


yarn add @nestjs/typeorm --save
yarn install --production=false

Шварцмиллер

yarn add redux react-redux redux-saga
yarn add -D react-logger
yarn add -D redux-logger


spinner на каждый запрос и возле каждого элемента


import dotProp from "dot-prop-immutable"; ???

mark-comlete
unmark-comlete


pre, post имена?

добавить валидацию на беке

изменение статуса через задачи POST(PUT(?)) /tasks/123/tasks

toggle

spinner вешать на любое дейтсвие, чтоб другие не могли выполняться

"отправить комманду", "отреагировать на результат комманды"

ошибки через reducer тоже

общий воркер просто принемает большей набор параметров

создание криво работает, после отключения api. Может те данные тоже в redux перенести?
